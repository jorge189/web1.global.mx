import { FileControlValueDirective } from './file-control-value.directive';

describe('FileControlValueDirective', () => {
  it('should create an instance', () => {
    const directive = new FileControlValueDirective();
    expect(directive).toBeTruthy();
  });
});
